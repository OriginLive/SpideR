# SpideR


A web crawler engine, that gathers data and outputs it in a user defined way and format.


##Example

Compile and run the program. When prompted, type the following command:
```
connect www.google.com
```

to connect to your desired web page. Without options the program outputs text into **output.txt**,
while going 3 found urls deep.
